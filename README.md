# HangfirePOC
Hangfire acts as both a queueing system and a background worker (processing) system in .NET environments. It manages the entire lifecycle of a background job, from the moment it is created until it is processed and completed. Use /hangfire or /dashboards to navigate to hangfire UI PrepareSchemaIfNecessary = true will create the necessary schemas in sql server
